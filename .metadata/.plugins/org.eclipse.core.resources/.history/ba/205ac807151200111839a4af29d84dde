package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.ApplicationContext;

import com.example.demo.di01.TV;

@SpringBootTest // spring 컨테이너 구성
public class DiTest {
	
	@Autowired ApplicationContext context; // spring 컨테이너
//	@Autowired TV tv; // 컨테이너에서 해당 인터페이스의 구현객체를 찾아서 주입.(필드 주입)
	
	@Test
	public void test() {
//		TV tv = Beanfactory.getBean("sm");
//		TV tv = context.getBean(TV.class); // tv.class - interface 구현체
		TV tv = (TV)context.getBean(("tv")); // tv.class - interface 구현체
		tv.powerOn();
		tv.volumeUp();
	}
	
}
